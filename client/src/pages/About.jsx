const About = () => {
  return (
    <div>
      <h2>About</h2>
      <p>This app is used to check spam messages. The model is trained on a dataset of labeled messages to accurately classify them as spam or not spam. The app is built using React and leverages the DeepDetect API for model inference.</p>
    </div>
  );
};

export default About;
